
 
   function mulTable(number){
    for(let index=1;index<=10;index++){
        
        let newTable= number*index
        
        console.log(`${number} * ${index} = ${newTable}`);
   }
   }
   mulTable(3);
    
    

